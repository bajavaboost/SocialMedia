server:
  port: 8889

spring:
  application:
    name: config-server-git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/bajavaboost/Java8ConfigGitServer.git
          default-label: main
          skip-ssl-validation: true
          username: bajavaboost
          password: ${java8configpassword}